## 1. Write a program to check if a number is positive.

```python
num = int(input("Enter the number"))
if num > 0:
    print("The number is Positive")
```

Output:-
```text
Enter the number -10
```

```text
Enter the number 12
The number is Positive
```

---

## 2. Write a program to check if a number is negative.
```python
num = int(input("Enter the number"))
if num < 0:
    print("The number is Negative")
```

Output:-
```text
Enter the number -10
The number is Negative
```

```text
Enter the number 12
```

---

## 3. Write a program to check if a number is greater than 100.

Program 1:-
```python
num = int(input("Enter a number: "))
if num > 100:
    print("The Number is greater than 100")
```

Program 2:-
```python
num = int(input("Enter a number: "))

if num - 100 > 0:
    print("The Number is greater than 100")
```

Output:-
```text
Enter a number: 89
```

```text
Enter the number: 120
The Number is greater than 100
```

---

## 4. Write a program to check if a number is divisible by 5.

Program 1:-
```python
num = int(input("Enter a number: "))
if num % 5 == 0:
    print("The number is divisible by 5")
```

Program 2:-
```python
num = int(input("Enter a number: "))

if (num // 5) * 5 == num:
    print("The number is divisible by 5")
```

Output:-
```text
Enter a number: 89
```

```text
Enter a number: 120
The number is divisible by 5
```

---

## 5. Write a program to check if a number is even.

Program 1:-
```python
num = int(input("Enter a number: "))
if num % 2 == 0:
    print("The Number is Even")
```

Program 2:-
```python
num = int(input("Enter a number: "))

if (num // 2) * 2 == num:
    print("The Number is Even")
```

Output:-
```text
Enter a number: 89
```

```text
Enter a number: 120
The number is Even
```

---

## 6. Write a program to check if a person is eligible to vote

Program 1:-
```python
age = int(input("Enter the Age"))
if age >= 18:
    print("The person is eligible to vote")
```

Program 2:-
```python
age = int(input("Enter the Age"))
if age - 18 > 0:
    print("The person is eligible to vote")
```

Output:-
```text
Enter the age 20
The person is eligible to vote
```
```text
Enter the age 8
```

---

## 7. Write a program to check if a character is a vowel.
```python
vowel = input("Enter the character")
if vowel == 'a' or vowel == 'e' or vowel == 'i' or vowel == 'o' or vowel == 'u' or vowel == 'A' or vowel == 'E' or vowel == 'I' or vowel == 'O' or vowel == 'U':
    print("It is a vowel")
```

Output:-
```text
Enter the character U
It is a vowel
```
```text
Enter the character a
It is a vowel
```
```text
Enter the character Q
```

---

## 8. Write a program to check if a given year is a leap year
```python
year = int(input("Enter the year"))
if (year % 100 == 0 and year % 400 == 0 and year % 4 == 0) or (year % 4 == 0 and year % 100 != 0):
    print("It is leap year")
```

Output:-
```text
Enter the year 2000
It is leap year
```
```text
Enter the year 1900
```
```text
Enter the year 1904
It is leap year
```

## Write a program to check if a number is a multiple of both 3 and 7.
```python
num = int(input("Enter the number"))
if num % 3 == 0 and num % 7 == 0:
    print(f"{num} is divisible by both 3 and 7")
```

Output:-
```text
Enter the number 21
21 is divisible by both 3 and 7
```
```text
Enter the number 18
```

## 10. Write a program to check if a string contains more than 5 characters.
```python
Word = input("Enter the string")
if len(Word) > 5:
    print("The string contains more than 5 characters")
```

Output:-
```text
Enter the string qw
```
```text
Enter the string sahana
The string contains more than 5 characters
```
